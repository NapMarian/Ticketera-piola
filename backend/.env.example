# Server
PORT=3000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=cteam_ticketera
DB_USER=root
DB_PASSWORD=your_password

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_EXPIRES_IN=7d

# Email (SMTP/IMAP - CTeam Global)
SMTP_HOST=smtp.cteamglobal.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=ticketera@cteamglobal.com
SMTP_PASS=your-email-password
EMAIL_FROM=C-Team Ticketera <ticketera@cteamglobal.com>

# IMAP (for incoming emails - optional)
IMAP_HOST=imap.cteamglobal.com
IMAP_PORT=993
IMAP_USER=ticketera@cteamglobal.com
IMAP_PASS=your-email-password
IMAP_TLS=true

# Frontend URL (for emails)
FRONTEND_URL=http://localhost:5173
