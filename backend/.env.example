# Server
PORT=3000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=cteam_ticketera
DB_USER=root
DB_PASSWORD=your_password

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_EXPIRES_IN=7d

# Email - Option 1: Resend (recommended for cloud)
# If RESEND_API_KEY is set, Resend will be used
# RESEND_API_KEY=re_xxxxxxxxxxxx

# Email - Option 2: SMTP (if no Resend key)
SMTP_HOST=mail.cteamglobal.com
SMTP_PORT=465
SMTP_USER=soporte@cteamglobal.com
SMTP_PASS=your-email-password
EMAIL_FROM=C-Team Soporte <soporte@cteamglobal.com>

# Frontend URL (for emails)
FRONTEND_URL=http://localhost:5173
